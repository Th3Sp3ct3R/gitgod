#!/bin/sh
# Optional: run tests before push. Off by default (large repo / slow CI).
[ "${GITGOD_PRE_PUSH_TEST:-}" != "1" ] && exit 0
cd "$(git rev-parse --show-toplevel)" 2>/dev/null || exit 0
[ -f package.json ] || exit 0
command -v npm >/dev/null 2>&1 || exit 0
npm test -s
